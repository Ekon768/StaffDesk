export default function Page(){return <Section title="Integrations" sub="Connection states are demo data until real OAuth/API credentials are configured."><div className="grid-auto">{[["Discord","Sync moderation and member data."],["Minecraft / Paper","Receive server moderation events through the StaffDesk plugin/API."],["Webhooks","Connect external services with signed webhook events."]].map(x=><div className="card" style={{padding:22}} key={x[0]}><div style={{display:"flex",justifyContent:"space-between"}}><h3>{x[0]}</h3><span className="badge">Not connected</span></div><p className="muted">{x[1]}</p><button className="btn btn-primary">Configure</button></div>)}</div></Section>} function Section(p:any){return <><div style={{marginBottom:22}}><div className="tiny">COMMUNITY</div><h1>{p.title}</h1><p className="muted">{p.sub}</p></div>{p.children}</>}