import Link from "next/link"; export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main style={{minHeight:"100vh",padding:"50px 20px"}}><div className="card" style={{maxWidth:650,margin:"auto",padding:28}}><div className="tiny">{slug.toUpperCase()} / PUNISHMENT APPEAL</div><h1>Appeal a punishment</h1><p className="muted">Provide enough context for staff to review your appeal.</p><div style={{display:"grid",gap:15,marginTop:25}}><input className="input" placeholder="Username"/><input className="input" placeholder="Punishment ID"/><textarea className="input" rows={5} placeholder="Reason for appeal"/><textarea className="input" rows={4} placeholder="Additional information or evidence"/><button className="btn btn-primary">Submit appeal</button><Link className="btn" href={"/c/"+slug}>Back to community</Link></div></div></main>}