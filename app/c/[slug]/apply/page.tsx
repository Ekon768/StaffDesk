import Link from "next/link"; export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main style={{minHeight:"100vh",padding:"50px 20px"}}><div className="card" style={{maxWidth:650,margin:"auto",padding:28}}><div className="tiny">{slug.toUpperCase()} / STAFF APPLICATION</div><h1>Staff Application</h1><p className="muted">Complete the form below. Submissions are reviewed by authorized staff.</p><div style={{display:"grid",gap:15,marginTop:25}}><input className="input" placeholder="Minecraft username"/><input className="input" placeholder="Discord username"/><textarea className="input" rows={4} placeholder="Why should we choose you?"/><textarea className="input" rows={4} placeholder="What does good moderation mean to you?"/><button className="btn btn-primary">Submit application</button><Link className="btn" href={"/c/"+slug}>Back to community</Link></div></div></main>}