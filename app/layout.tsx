import "./globals.css"; import type { Metadata } from "next"; export const metadata:Metadata={title:"StaffDesk — Your community's staff, organized.",description:"Professional staff management for Discord, Minecraft, and gaming communities."}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en" className="dark"><body>{children}</body></html>}